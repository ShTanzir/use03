<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" 
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MODFLOW"
        tools:targetApi="34">

        <activity
            android:name=".ui.dashboard.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.MODFLOW">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.projects.ProjectActivity"
            android:exported="false"
            android:parentActivityName=".ui.dashboard.MainActivity" />

        <activity
            android:name=".ui.analyzer.AnalyzerActivity"
            android:exported="false"
            android:parentActivityName=".ui.projects.ProjectActivity" />

        <activity
            android:name=".ui.explorer.ExplorerActivity"
            android:exported="false"
            android:parentActivityName=".ui.projects.ProjectActivity" />

        <activity
            android:name=".ui.smali.SmaliEditorActivity"
            android:exported="false"
            android:parentActivityName=".ui.explorer.ExplorerActivity" />

        <activity
            android:name=".ui.workflow.WorkflowBuilderActivity"
            android:exported="false"
            android:parentActivityName=".ui.projects.ProjectActivity" />

        <activity
            android:name=".ui.recipes.RecipeActivity"
            android:exported="false"
            android:parentActivityName=".ui.dashboard.MainActivity" />

        <activity
            android:name=".ui.settings.SettingsActivity"
            android:exported="false"
            android:parentActivityName=".ui.dashboard.MainActivity" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
